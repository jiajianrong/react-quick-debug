import React from 'react';

export default class D extends React.Component {
  
    
    
    render() {
        
        return (
            <div>D contains : &nbsp; {this.props.children} </div>
        );
    }
}